/**
 * @typedef {import('distube').DisTubeEvents} DisTubeEvents
 *
 * @typedef {Object} IEvent
 * @property {keyof DisTubeEvents} name
 * @property {(...args: any[]) => void} function
 * @property {?boolean} once
 */

module.exports = {};
